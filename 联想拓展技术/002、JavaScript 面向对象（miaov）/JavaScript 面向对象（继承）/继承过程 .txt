对象组成：
	属性：
	方法：

继承对象：
	继承的就是
		属性：
		方法：

继承过程：
	先执行父级的构造函数（将属性继承过来），然后在添加自己单独的属性。继承父类的方法。
	1、使用call 或 apply 进行伪装。继承构造函数。
	2、使用for in 和原型链的继承，继承原型上的方法。
	注意： a、子类需要先进行继承父类的属性，然后在添加自己的属性。
		 b、子类需要先进行继承父类的方法，然后在添加自己的方法。
		
优点：
	如果整个对象有些bug，我们只需要在父类上进行修改，就可以了，比较好。
