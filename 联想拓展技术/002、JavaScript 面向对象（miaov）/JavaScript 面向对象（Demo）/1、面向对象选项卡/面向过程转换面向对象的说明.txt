第一步
	1、前提：所有东西都在onload里面
	2、改写：函数不能套用函数，需要提取出来。
	3、可以有全局变量
	4、改错。将多次使用的定义为全局使用
第二部
	//使用面向对象拖拽的话我们要考虑
	//1、onload改成构造函数
	//2、全局变量 改成对象的属性
	//3、函数改成对象的方法
	//4、将构造函数的形参添加上以便多个实例使用
	//5、this的添加  注意this的指向，多测试如果有 定时器和事件那么 使用 var _this = this; 多加一层
	//6、实例化对象，传参数。